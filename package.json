{
  "name": "ha-intercom-card",
  "version": "0.1.0",
  "private": true,
  "description": "Intercom card and settings card for the Home Assistant intercom integration",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@mdi/svg": "^7.4.47",
    "esbuild": "^0.25.0",
    "lit": "^3.2.0"
  }
}
